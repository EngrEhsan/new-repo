run_tests:
  image: ubuntu:latest
  before_script:
    - apt-get update && apt-get install -y build-essential
    - apt-get install -y cmake make
    
  script:
    - mkdir build && cd build
    - cmake ../
    - make

  artifacts:
    paths:
      - build/main
